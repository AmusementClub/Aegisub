add_executable(audio-display-bench EXCLUDE_FROM_ALL
    tools/audio_display_bench_main.cpp
    src/audio_display_analysis.cpp
    src/audio_latest_range_scheduler.cpp
    src/audio_display_source.cpp
    src/audio_mix_policy.cpp
    src/audio_renderer.cpp
    src/audio_spectrum_analysis_cache.cpp
    src/audio_waveform_summary_cache.cpp
    src/fft.cpp
)
target_include_directories(audio-display-bench PRIVATE
    "${PROJECT_SOURCE_DIR}/src"
    "${PROJECT_SOURCE_DIR}/libaegisub/include"
)
target_link_libraries(audio-display-bench PRIVATE libaegisub ${wxWidgets_LIBRARIES})
aegisub_enable_highway(audio-display-bench)

if(EXISTS "${PROJECT_SOURCE_DIR}/tools/string_concat_bench_main.cpp")
    add_executable(string-concat-bench EXCLUDE_FROM_ALL
        tools/string_concat_bench_main.cpp
    )
    target_compile_features(string-concat-bench PRIVATE cxx_std_20)
    target_include_directories(string-concat-bench PRIVATE
        "${PROJECT_SOURCE_DIR}/libaegisub/include"
    )
    if(STRINGZILLA_INCLUDE_DIRS)
        target_include_directories(string-concat-bench PRIVATE ${STRINGZILLA_INCLUDE_DIRS})
        target_compile_definitions(string-concat-bench PRIVATE AEGISUB_USE_STRINGZILLA=1)
    endif()
    target_link_libraries(string-concat-bench PRIVATE "Boost::headers")
endif()

if(EXISTS "${PROJECT_SOURCE_DIR}/tools/string_path_bench_main.cpp")
    add_executable(string-path-bench EXCLUDE_FROM_ALL
        tools/string_path_bench_main.cpp
    )
    target_compile_features(string-path-bench PRIVATE cxx_std_20)
    target_include_directories(string-path-bench PRIVATE
        "${PROJECT_SOURCE_DIR}/libaegisub/include"
    )
    if(STRINGZILLA_INCLUDE_DIRS)
        target_include_directories(string-path-bench PRIVATE ${STRINGZILLA_INCLUDE_DIRS})
        target_compile_definitions(string-path-bench PRIVATE AEGISUB_USE_STRINGZILLA=1)
    endif()
    target_link_libraries(string-path-bench PRIVATE "Boost::headers" "Boost::locale")
endif()

if(EXISTS "${PROJECT_SOURCE_DIR}/tools/phase1_low_cost_bench_main.cpp")
    add_executable(phase1-low-cost-bench EXCLUDE_FROM_ALL
        tools/phase1_low_cost_bench_main.cpp
    )
    target_compile_features(phase1-low-cost-bench PRIVATE cxx_std_20)
    target_include_directories(phase1-low-cost-bench PRIVATE
        "${PROJECT_SOURCE_DIR}/libaegisub/include"
    )
    target_link_libraries(phase1-low-cost-bench PRIVATE "Boost::headers")
endif()

add_executable(modern-gl-renderer-tile-bench EXCLUDE_FROM_ALL
    tools/video_renderer_opengl_tile_bench_main.cpp
    src/video_renderer_opengl_tile.cpp
)
target_compile_features(modern-gl-renderer-tile-bench PRIVATE cxx_std_20)
target_include_directories(modern-gl-renderer-tile-bench PRIVATE
    "${PROJECT_SOURCE_DIR}/src"
    "${PROJECT_SOURCE_DIR}/libaegisub/include"
)
target_link_libraries(modern-gl-renderer-tile-bench PRIVATE "Boost::headers")

add_executable(modern-gl-overlay-plan-bench EXCLUDE_FROM_ALL
    tools/video_renderer_opengl_overlay_upload_plan_bench_main.cpp
)
target_compile_features(modern-gl-overlay-plan-bench PRIVATE cxx_std_20)
target_include_directories(modern-gl-overlay-plan-bench PRIVATE
    "${PROJECT_SOURCE_DIR}/src"
    "${PROJECT_SOURCE_DIR}/libaegisub/include"
)
target_link_libraries(modern-gl-overlay-plan-bench PRIVATE "Boost::headers")

add_executable(dispatch-micro-bench EXCLUDE_FROM_ALL
    tools/dispatch_micro_bench_main.cpp
)
target_compile_features(dispatch-micro-bench PRIVATE cxx_std_20)
target_include_directories(dispatch-micro-bench PRIVATE
    "${PROJECT_SOURCE_DIR}/libaegisub/include"
)
target_link_libraries(dispatch-micro-bench PRIVATE libaegisub)

add_executable(drawing-bench EXCLUDE_FROM_ALL
    tools/drawing_bench_main.cpp
)
target_compile_features(drawing-bench PRIVATE cxx_std_20)
target_include_directories(drawing-bench PRIVATE
    "${PROJECT_SOURCE_DIR}/libaegisub/include"
)
target_link_libraries(drawing-bench PRIVATE libaegisub)

if(NOT ass_LIBRARIES)
    message(STATUS "drawing-compare: ass link library not found; benchmark target disabled")
else()
    add_executable(drawing-compare EXCLUDE_FROM_ALL
        tools/drawing_compare_main.cpp
    )
    target_compile_features(drawing-compare PRIVATE cxx_std_20)
    target_include_directories(drawing-compare PRIVATE
        "${PROJECT_SOURCE_DIR}/libaegisub/include"
        "${PROJECT_SOURCE_DIR}/vendor/csri/include"
        ${ass_INCLUDE_DIRS}
    )
    target_link_libraries(drawing-compare PRIVATE libaegisub ${ass_LIBRARIES} ${CMAKE_DL_LIBS})
endif()

if(EXISTS "${PROJECT_SOURCE_DIR}/tools/dispatch_realworld_bench_main.cpp")
    add_executable(dispatch-realworld-bench EXCLUDE_FROM_ALL
        tools/dispatch_realworld_bench_main.cpp
    )
    target_compile_features(dispatch-realworld-bench PRIVATE cxx_std_20)
    target_include_directories(dispatch-realworld-bench PRIVATE
        "${PROJECT_SOURCE_DIR}/libaegisub/include"
    )
    target_link_libraries(dispatch-realworld-bench PRIVATE libaegisub)
endif()

add_executable(video-cache-bench EXCLUDE_FROM_ALL
    tools/video_cache_bench_main.cpp
    src/video_provider_cache.cpp
)
target_compile_features(video-cache-bench PRIVATE cxx_std_20)
target_include_directories(video-cache-bench PRIVATE
    "${PROJECT_SOURCE_DIR}/src"
    "${PROJECT_SOURCE_DIR}/libaegisub/include"
)
target_link_libraries(video-cache-bench PRIVATE libaegisub)

add_executable(async-video-provider-stress EXCLUDE_FROM_ALL
    tools/async_video_provider_stress_main.cpp
    src/ass_attachment.cpp
    src/ass_dialogue.cpp
    src/ass_entry.cpp
    src/ass_file.cpp
    src/ass_override.cpp
    src/ass_style.cpp
    src/ass_style_storage.cpp
    src/async_video_provider.cpp
    src/async_video_provider_host.cpp
    src/compat.cpp
    src/subtitle_overlay_blend.cpp
    src/utils.cpp
)
target_compile_features(async-video-provider-stress PRIVATE cxx_std_20)
target_include_directories(async-video-provider-stress PRIVATE
    "${PROJECT_SOURCE_DIR}/src"
    "${PROJECT_SOURCE_DIR}/libaegisub/include"
)
target_link_libraries(async-video-provider-stress PRIVATE libaegisub ${wxWidgets_LIBRARIES})
aegisub_enable_highway(async-video-provider-stress)

if(EXISTS "${PROJECT_SOURCE_DIR}/tools/phase2a_lex_token_bench_main.cpp")
    add_executable(phase2a-lex-token-bench EXCLUDE_FROM_ALL
        tools/phase2a_lex_token_bench_main.cpp
    )
    target_compile_features(phase2a-lex-token-bench PRIVATE cxx_std_20)
    target_include_directories(phase2a-lex-token-bench PRIVATE
        "${PROJECT_SOURCE_DIR}/libaegisub/include"
    )
    target_link_libraries(phase2a-lex-token-bench PRIVATE "Boost::headers")
endif()

if(EXISTS "${PROJECT_SOURCE_DIR}/tools/phase2a_algorithm_bench_main.cpp")
    add_executable(phase2a-algorithm-bench EXCLUDE_FROM_ALL
        tools/phase2a_algorithm_bench_main.cpp
    )
    target_compile_features(phase2a-algorithm-bench PRIVATE cxx_std_20)
    target_include_directories(phase2a-algorithm-bench PRIVATE
        "${PROJECT_SOURCE_DIR}/libaegisub/include"
    )
    target_link_libraries(phase2a-algorithm-bench PRIVATE "Boost::headers")
endif()

if(NOT TARGET Boost::filesystem)
    find_package(Boost QUIET COMPONENTS filesystem)
endif()

if(EXISTS "${PROJECT_SOURCE_DIR}/tools/phase2b_filesystem_bench_main.cpp")
    add_executable(phase2b-filesystem-bench EXCLUDE_FROM_ALL
        tools/phase2b_filesystem_bench_main.cpp
    )
    target_compile_features(phase2b-filesystem-bench PRIVATE cxx_std_20)
    target_include_directories(phase2b-filesystem-bench PRIVATE
        "${PROJECT_SOURCE_DIR}/libaegisub/include"
    )
    target_link_libraries(phase2b-filesystem-bench PRIVATE libaegisub)
    if(TARGET Boost::filesystem)
        target_compile_definitions(phase2b-filesystem-bench PRIVATE AEGISUB_BENCH_HAVE_BOOST_FILESYSTEM=1)
        target_link_libraries(phase2b-filesystem-bench PRIVATE "Boost::filesystem")
    endif()
endif()

add_executable(csri-overlay-bench EXCLUDE_FROM_ALL
    tools/csri_overlay_bench_main.cpp
    src/subtitle_overlay_blend.cpp
)
target_compile_features(csri-overlay-bench PRIVATE cxx_std_20)
target_include_directories(csri-overlay-bench PRIVATE
    "${PROJECT_SOURCE_DIR}/src"
    "${PROJECT_SOURCE_DIR}/libaegisub/include"
)
target_link_libraries(csri-overlay-bench PRIVATE "Boost::headers")
aegisub_enable_highway(csri-overlay-bench)

add_executable(bgra-transform-bench EXCLUDE_FROM_ALL
    tests/smoke/bgra_transform_bench.cpp
)
target_compile_features(bgra-transform-bench PRIVATE cxx_std_20)
target_include_directories(bgra-transform-bench PRIVATE
    "${PROJECT_SOURCE_DIR}/src"
    "${PROJECT_SOURCE_DIR}/libaegisub/include"
)
aegisub_enable_highway(bgra-transform-bench)

add_executable(key-point-color-bench EXCLUDE_FROM_ALL
    tools/key_point_color_bench_main.cpp
)
target_compile_features(key-point-color-bench PRIVATE cxx_std_20)
target_include_directories(key-point-color-bench PRIVATE
    "${PROJECT_SOURCE_DIR}/src"
)

find_package(benchmark CONFIG QUIET)
if(benchmark_FOUND)
    add_executable(audio-micro-bench EXCLUDE_FROM_ALL
        tools/audio_micro_bench_main.cpp
        src/audio_display_analysis.cpp
        src/audio_latest_range_scheduler.cpp
        src/audio_display_source.cpp
        src/audio_mix_policy.cpp
        src/audio_spectrum_analysis_cache.cpp
        src/audio_waveform_summary_cache.cpp
        src/fft.cpp
        src/skia/audio/skia_audio_content.cpp
        src/skia/audio/skia_audio_frame_model.cpp
    )
    target_include_directories(audio-micro-bench PRIVATE
        "${PROJECT_SOURCE_DIR}/src"
        "${PROJECT_SOURCE_DIR}/libaegisub/include"
    )
    target_link_libraries(audio-micro-bench PRIVATE benchmark::benchmark libaegisub ${wxWidgets_LIBRARIES})
    aegisub_enable_highway(audio-micro-bench)
endif()
