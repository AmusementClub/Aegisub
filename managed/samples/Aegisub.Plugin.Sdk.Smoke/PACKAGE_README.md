# Aegisub Plugin SDK smoke

This package is an integration fixture. Its `aegisub/` directory contains a
manifest v2 file and the matching CoreCLR plugin payload generated by the SDK
and BuildTasks projects.
